const marcador = (pontos) => {
    
    let pontosLista = [] = pontos
  
    
  
    
    for (let add in pontosLista){
        console.log(`${add} = ${pontosLista[add]}`)
    }
}

marcador("10 20 30 40")

//criar um objeto para conter a pontuacao e criar um método para
//verificar qual é a nota mais alta e a mais baixa, verificar 
//quantas vezes a nota ultrapassou o recorde.