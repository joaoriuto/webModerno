const [a, b, c, d] = [3, 5, 1, 15] //->Forma reduzida de se criar variaveis(const)

const soma = a + b + c + d //Binário
const subtracao = d - b //Binário
const multiplicacao = a * b //Binário
const divisao = d /a //Binário
const modulo = a % 2 //Binário

console.log(soma, subtracao, multiplicacao, divisao, modulo)